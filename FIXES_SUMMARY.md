# 양보 및 경로 계산 개선 사항

## 🔍 발견된 주요 문제점

### 1. **양보 대상 선택 무한 루프**
- **증상**: RD2가 `Storage_front`에서 계속 같은 위치로 양보 요청을 반복
- **원인**: 금지 구역이 너무 넓어서 양보할 위치 후보가 극도로 제한됨
- **해결**: 양보 위치를 찾을 수 없을 때 폴백 전략 추가 (`_find_yield_target_fallback`)

### 2. **양보 쿨타임이 너무 길어서 빈번한 재계산 유발**
- **증상**: 1.2초 대기 중에 동일한 충돌이 계속 발생하여 유효하지 않은 재계산 반복
- **원인**: `YIELD_COOLDOWN_MS = 1200` 설정이 게임 플레이상 너무 김
- **해결**: 쿨타임 단축: **1200ms → 300ms**

### 3. **금지 구역 설정 오류**
- **증상**: 양보자의 원래 경로 정보가 금지 구역에 반영되지 않아, 양보 후 원래 경로로 돌아가려는 문제
- **원인**: 양보자의 계획된 경로(blocker_path)를 금지 구역에 추가하지 않음
- **해결**: 양보자의 미래 경로도 금지 구역에 추가하여 흐름 방해 방지

### 4. **거리 계산 오류**
- **증상**: `d_from_blocker <= 0` 조건이 거리 0(현재 위치)도 제외
- **원인**: 현재 위치에서의 양보를 불가능하게 만드는 부등호 오류
- **해결**: 조건 변경: `if d_from_blocker <= 0` → `if d_from_blocker < 0`

### 5. **무한 양보 반복 (Yield Loop)**
- **증상**: 같은 충돌 지점에서 계속 양보 요청
- **원인**: 양보 시도 횟수 제한이 없음
- **해결**: 
  - `_yield_attempt_count` 변수 추가로 같은 충돌당 최대 5회 제한
  - `BLOCKED_REPLAN_MAX_TIMES = 5` 상수 추가
  - 최대 횟수 초과 시 더 긴 쿨타임(2초) 설정

### 6. **경로 찾기 실패 시 디버깅 정보 부족**
- **증상**: 경로 계산 실패 원인을 파악하기 어려움
- **원인**: 실패 시 단순히 `return`하기만 함
- **해결**: 
  - 실패 시 상세한 오류 메시지 출력
  - 양보 대상 선택 실패 시 "금지 구역이 너무 넓을 가능성" 진단 메시지 추가

### 7. **양보 거부 후 재요청 대기 시간 부족**
- **증상**: 양보가 거부되면 바로 다시 요청하여 불필요한 계산 반복
- **원인**: 양보 거부 시 쿨타임이 설정되지 않음
- **해결**: 양보 거부 시 추가 대기 시간 설정 (YIELD_REQUEST_COOLDOWN_SECONDS * 1.5)

## ✅ 적용된 수정 사항

### 📝 `res://Scripts/Autorun/MapfManager.gd`

1. **상수 수정**
   ```gdscript
   const YIELD_COOLDOWN_MS := 300    # 1200 → 300 (단축)
   const BLOCKED_REPLAN_MAX_TIMES := 5 # 신규 추가
   ```

2. **변수 추가**
   ```gdscript
   var _yield_attempt_count: Dictionary = {} # 양보 시도 횟수 추적
   ```

3. **함수 추가**
   - `_reset_yield_attempts_for_agent(agent_id)`: 에이전트의 양보 시도 기록 초기화
   - `_find_yield_target_fallback()`: 양보 위치 찾기 실패 시 폴백 전략

4. **함수 개선**
   - `_build_forbidden_nodes_for_yield()`: 양보자의 원래 경로도 금지 구역에 추가
   - `_find_yield_target()`: 거리 검사 조건 완화 & 디버깅 메시지 강화
   - `try_resolve_deadlock()`: 같은 충돌에 대한 양보 시도 횟수 제한 추가
   - `request_path()`: 새 경로 요청 시 양보 시도 기록 초기화

### 📝 `res://Scripts/Class/animatronics.gd`

1. **경로 찾기 실패 시 디버깅**
   ```gdscript
   if move_path.is_empty():
       printerr("[경로 실패] %s: %s → %s 경로를 찾을 수 없음" % [...])
   ```

2. **양보 거부 시 쿨타임 설정**
   ```gdscript
   else:
       _yield_request_cooldown = YIELD_REQUEST_COOLDOWN_SECONDS * 1.5
   ```

## 🎯 개선 효과

| 문제 | 이전 | 현재 | 효과 |
|------|------|------|------|
| 양보 요청 재시도 간격 | 1200ms | 300ms | 빠른 문제 해결 |
| 양보 대상 없을 때 | 계속 재요청 | 폴백 전략 + 제한 | 무한 루프 방지 |
| 같은 충돌 반복 | 무제한 | 최대 5회 | 명백한 교착상태 회피 |
| 경로 실패 원인 파악 | 어려움 | 상세 로그 | 디버깅 용이 |
| 금지 구역 정확도 | 불완전 | 완전 | 양보 후 원래 경로로 돌아가는 문제 해결 |

## 📋 테스트 체크리스트

- [ ] RD1과 RD2가 같은 경로에서 만날 때 부자연스러운 멀리 양보 X
- [ ] 양보 후 원래 경로로 복귀하는 로직 동작 확인
- [ ] 경로 찾기 실패 시 오류 메시지 출력 확인
- [ ] 프레임 드롭 없이 부드러운 이동 확인
- [ ] 콘솔 로그가 지나치게 많지 않은지 확인 (1-2초에 10개 이상 로그 X)

## 🔔 주의 사항

- 양보 쿨타임이 짧아졌으므로, 마맵에서 너무 좁은 통로가 있으면 교착상태 가능성 증가
  → 이 경우 마맵 설계 검토 필요
- 양보 시도 횟수 제한(5회)은 필요시 조정 가능: `BLOCKED_REPLAN_MAX_TIMES`

## 💡 향후 개선 사항

1. **동적 우선순위**: 현재는 초기 우선순위 고정 → 실시간으로 우선순위 재계산
2. **경로 예약 갱신**: 양보 후 새로운 경로로 자동 재계산
3. **시뮬레이션 기반 선택**: 양보 위치의 실제 이동 시간 예측 후 최적 선택
4. **통로 분석**: 자주 충돌하는 지역 자동 감지 → 마맵 설계 개선 제안
